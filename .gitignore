# Playwright MCP runtime cache (browser snapshots, console logs, traces)
.playwright-mcp/

# Claude Code per-user settings (machine-local; do not share)
.claude/settings.local.json

# OS / editor noise
.DS_Store
*.swp
*.swo
*~

# Phase 6b — regenerator artifacts
# The candidate blueprint is regenerated during the soak; reviewed via diff
# rather than committed. The pre-regen backup is created by `tools/build-blueprint.sh --write`.
SYSTEM-BLUEPRINT.candidate.md
.SYSTEM-BLUEPRINT.pre-regen.*.md
